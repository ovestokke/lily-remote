# Local configuration

1. Copy `config.example.h` to `config.h`.
2. Fill in WiFi and Home Assistant values.
3. Keep `config.h` private — it is ignored by git.

```bash
cp include/config.example.h include/config.h
```
